import 'package:flutter/material.dart';

class RingChatView extends StatelessWidget {
  const RingChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
